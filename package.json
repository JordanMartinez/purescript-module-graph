{
  "name": "purescript-module-graph",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "client:build": "spago -x client.dhall build",
    "client:build-watch": "spago -x client.dhall build -w",
    "client:run": "spago -x client.dhall bundle-app --main Client.Main --to dist/app.js",
    "client:run-watch": "spago -x client.dhall bundle-app -w --main Client.Main --to dist/client/app.js",
    "server:build": "spago -x server.dhall build",
    "server:run": "spago -x server.dhall run -m Server.Main",
    "both:build":  "spago build",
    "both:build-watch":  "spago build"
  },
  "license": "MIT"
}
